#### 📑 Краткое описание
Устаревший бэкенд для videoControl

REST API сервис для обработки заявок с отправкой уведомлений в Telegram и по email.

---

#### 🚀 Технологии

- Node.js + TypeScript
- Express.js - веб-фреймворк
- Drizzle ORM - работа с базой данных
- PostgreSQL - база данных
- Nodemailer - отправка email
- node-telegram-bot-api - интеграция с Telegram
- Nodemon + tsx - разработка
- GitHub Actions CI/CD - Деплой (есть в репозитории)
---
#### ⚙️ Установка

1. Клонировать репозиторий:

	```bash
	git clone https://github.com/Tuxi4k/videoBackendOld.git
	cd videoBackendOld
	```

2. Установить зависимости:
	- npm:
		```bash
		npm install
		```

	- pnpm (рекомендуется):
		```bash
		pnpm install
		```

3. Настройка окружения:

	Настройка переменных окружения
	
	В репозитории присутствуют примеры конфигурационных файлов:
	
	-  .dev.env.example - для разработки
	-  .prod.env.example - для продакшена
	
	Не используйте эти файлы напрямую!
	
	Создайте файл .env в корне проекта и скопируйте туда настройки из соответствующего примера:

	```bash
	# Для разработки
	cp .dev.env.example .env
	
	# Или для продакшена
	cp .prod.env.example .env
	```

	Затем отредактируйте файл .env под ваше окружение.


4. Инициализация базы данных:

	```bash
	npm run db:generate
	npm run db:push
	```

5. 🏃‍♂️ Запуск

	Разработка
	
	```bash
	npm run dev:db # postgress-embedded
	```
	
	```bash
	npm run dev
	```
	
	Сборка
	
	```bash
	npm run build
	```
	
	Команды
	
	```bash
	npm run dev          # Запуск в режиме разработки
	npm run dev:db     # запуск postgress embedded
	npm run build        # Сборка проекта
	npm start           # Запуск в продакшене
	npm run test        # Запуск тестовой сборки
	npm run db:push     # Создание изменение дб
	```
---
#### 🔄 CI/CD Pipeline

В проекте настроен CI/CD pipeline с использованием GitHub Actions. Конфигурация находится в .github/workflows/main.yml

Этапы pipeline:

1. Build - сборка TypeScript кода
2. Deploy - деплой на production сервер
---
#### 📝 Лицензия

MIT License. Подробнее в файле LICENSE.

---

#### ⚠️ Важно

- Файл .env используется для всех окружений и должен быть создан вручную
- Для CI/CD используйте GitHub Secrets для хранения переменных окружения
- Разработка остановлена на совсем (репозиторий не будет обновляться)
